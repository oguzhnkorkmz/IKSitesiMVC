@using IKSitesiMVC.Areas.SirketAdmin.ViewModels.Talep
@model IEnumerable<IzinTalebiList_VM>

@{
    ViewData["Title"] = "Bekleyen İzin Talepleri";
}

<h2>@ViewData["Title"]</h2>


<table class="table">
    <thead>
        <tr>
            <th>Personel</th>
            <th>Başlangıç Tarihi</th>
            <th>Bitiş Tarihi</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var talep in Model)
        {
            <tr>
                <td>@talep.PersonelAdi</td>
                <td>@talep.BaslangicTarihi.ToShortDateString()</td>
                <td>@talep.BitisTarihi.ToShortDateString()</td>
                <td>
                    <form asp-action="Onayla" method="post" style="display:inline;">
                        <input type="hidden" name="izinTalebiID" value="@talep.IzinTalebiID" />
                        <input type="hidden" name="kurumId" value="@talep.KurumID" />
                        <button type="submit" class="btn btn-success">Onayla</button>
                    </form>
                    <form asp-action="Reddet" method="post" style="display:inline; margin-left:10px;">
                        <input type="hidden" name="izinTalebiID" value="@talep.IzinTalebiID" />
                        <input type="hidden" name="kurumId" value="@talep.KurumID" />
                        <button type="submit" class="btn btn-danger">Reddet</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>